{
  "name": "@opspilot/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "lint": "eslint src --max-warnings=0",
    "test": "vitest run --passWithNoTests",
    "test:coverage": "vitest run --coverage --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@opspilot/config": "workspace:*",
    "drizzle-orm": "^0.43.1",
    "pg": "^8.16.0"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0"
  }
}
